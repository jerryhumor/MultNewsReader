<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/news_list_item_height"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/iv_logo"
        android:layout_width="@dimen/news_list_item_logo_width"
        android:layout_height="@dimen/news_list_item_logo_width"
        android:layout_margin="@dimen/news_list_item_margin"
        tools:src="@mipmap/logo_netease"/>

    <LinearLayout
        android:id="@+id/ll_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_toRightOf="@id/iv_logo">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3"
            android:layout_margin="@dimen/news_list_item_title_margin"
            android:maxLines="1"
            tools:text="今天袜子大减价"/>

        <TextView
            android:id="@+id/tv_time"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="2"
            android:layout_marginLeft="20dp"
            android:layout_marginBottom="8dp"
            android:textSize="12sp"
            tools:text="2017-11-9"/>

    </LinearLayout>

</RelativeLayout>